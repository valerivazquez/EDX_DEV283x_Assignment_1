var assert = require('assert');
describe('CSV to JSON Converter', function() {
  describe('Verify csv File', function() {
    it('the file exist', function(){
      assert.equal(-1, [1,2,3].indexOf(4));
    });
  });
});